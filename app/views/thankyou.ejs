<!DOCTYPE html>

<html xmlns="http://www.w3.org/1999/xhtml">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <title>COMP308-Lab1 :: Thank you</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.8.2/css/bulma.min.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="/">COMP308-Ass02</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <% if (isAdmin) { %>
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/view-feedback">View Feedback</a>
                </li>
            </ul>
            <% } %>
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/">Login</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/signup">Register</a>
                </li>
            </ul>
        </div>
    </nav>
    <div class="container mt-4">
        <div class="row">
            <div class="col-12">
                <div class="alert alert-secondary">
                    <%if (name.length == 0) { %>
                    <h4>Thank you for your feedback!</h4>
                    <% } else { %>
                    <h4>Thank you for your feedback, <%= name %>!</h4>
                    <% } %>
                    <div style="font-weight: bold; font-size: 17px;" class="mt-4">
                        Those were your comments:
                    </div>
                    <div class="mt-1">
                        <%= comments %>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>